import React, {
  FunctionComponent,
  useState
} from 'react'
import {
  act,
  cleanup,
  fireEvent,
  render
} from '@testing-library/react'

afterEach(cleanup)

describe('Test React', () => {
  it('should work correctly', () => {
  })
})
