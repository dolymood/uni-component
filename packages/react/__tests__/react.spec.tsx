import React, {
  FunctionComponent,
  useState
} from 'react'
import {
  act,
  cleanup,
  fireEvent,
  render
} from '@testing-library/react'
import { CubeButton } from '../example/button/button'
import { CubeButtons } from '../example/buttons/buttons'

afterEach(cleanup)

describe('Test React', () => {
  it('should work correctly - with button', () => {
    
  })
  it('should work correctly - with buttons', () => {
  })
  it('should work correctly - with buttons & button', () => {
  })
})
