import {
  PropType,
  UniNode,
  uniComponent
} from '@uni-component/core'

export const CubeTip = uniComponent('cube-tip', {
  defaultRender: Function as PropType<() => UniNode | undefined>
}, (name, props) => {
  return {
  }
})
